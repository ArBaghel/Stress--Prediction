# =========================================================
# requirements.txt — Stress Detection ECG (CNN-BiLSTM-DACAM+)
# Python 3.10 | Windows | Intel Arc (Ultra-5/7 125H)
# =========================================================

# ── TensorFlow + Intel Arc GPU (DirectML) ─────────────────
# tensorflow-directml-plugin ONLY works with the 2.10.x family.
# DO NOT upgrade to 2.11+ or the plugin will fail.
tensorflow==2.10.0
tensorflow-directml-plugin==0.4.0.dev230202

# ── protobuf — CRITICAL: must be <3.20 for TF 2.10 ───────
# protobuf 3.20+ introduced `runtime_version` which TF 2.10 does NOT use,
# causing the "cannot import name 'runtime_version'" ImportError.
protobuf==3.19.6

# ── Scientific stack ──────────────────────────────────────
numpy==1.24.3
pandas==2.0.3
matplotlib==3.7.2
seaborn==0.12.2
scipy==1.11.1

# ── ECG / biosignal ───────────────────────────────────────
wfdb==4.1.2
neurokit2==0.2.7

# ── ML utilities ─────────────────────────────────────────
scikit-learn==1.3.0
imbalanced-learn==0.11.0

# ── App / notebook ────────────────────────────────────────
streamlit==1.22.0
opencv-python==4.8.0.76
pillow==9.5.0
ipykernel==6.25.0
