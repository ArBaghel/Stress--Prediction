# =========================================================
# requirements.txt — Stress Detection ECG (CNN-BiLSTM-DACAM+)
# =========================================================

# ── TensorFlow & GPU settings ─────────────────────────────
# tensorflow-directml-plugin ONLY works with TensorFlow 2.10.x family.
# On Windows (local environment with Intel Arc GPU):
tensorflow-directml-plugin==0.4.0.dev230202; sys_platform == 'win32'
# On Streamlit Cloud (Linux) or other platforms:
tensorflow-cpu==2.10.0

# ── protobuf — CRITICAL: must be <3.20 for TF 2.10 ───────
protobuf==3.19.6

# ── Scientific stack ──────────────────────────────────────
numpy==1.24.3
pandas==2.0.3
matplotlib==3.7.2
seaborn==0.12.2
scipy==1.11.1

# ── ECG / biosignal ───────────────────────────────────────
wfdb==4.1.2
neurokit2==0.2.7

# ── ML utilities ─────────────────────────────────────────
scikit-learn==1.3.0
imbalanced-learn==0.11.0

# ── App / notebook ────────────────────────────────────────
streamlit==1.22.0
pillow==9.5.0
ipykernel==6.25.0

# Headless OpenCV for server environment (Streamlit Cloud)
opencv-python==4.8.0.76; sys_platform == 'win32'
opencv-python-headless==4.8.0.76; sys_platform != 'win32'
